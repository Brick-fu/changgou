<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.changgou.user.dao.TbAreasDao">

    <resultMap type="com.changgou.user.pojo.TbAreas" id="TbAreasMap">
        <result property="areaid" column="areaid" jdbcType="VARCHAR"/>
        <result property="area" column="area" jdbcType="VARCHAR"/>
        <result property="cityid" column="cityid" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TbAreasMap">
        select
          areaid, area, cityid
        from tb_areas
        where areaid = #{areaid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TbAreasMap">
        select
          areaid, area, cityid
        from tb_areas
        <where>
            <if test="areaid != null and areaid != ''">
                and areaid = #{areaid}
            </if>
            <if test="area != null and area != ''">
                and area = #{area}
            </if>
            <if test="cityid != null and cityid != ''">
                and cityid = #{cityid}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from tb_areas
        <where>
            <if test="areaid != null and areaid != ''">
                and areaid = #{areaid}
            </if>
            <if test="area != null and area != ''">
                and area = #{area}
            </if>
            <if test="cityid != null and cityid != ''">
                and cityid = #{cityid}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="areaid" useGeneratedKeys="true">
        insert into tb_areas(area, cityid)
        values (#{area}, #{cityid})
    </insert>

    <insert id="insertBatch" keyProperty="areaid" useGeneratedKeys="true">
        insert into tb_areas(area, cityid)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.area}, #{entity.cityid})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="areaid" useGeneratedKeys="true">
        insert into tb_areas(area, cityid)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.area}, #{entity.cityid})
        </foreach>
        on duplicate key update
        area = values(area),
        cityid = values(cityid)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update tb_areas
        <set>
            <if test="area != null and area != ''">
                area = #{area},
            </if>
            <if test="cityid != null and cityid != ''">
                cityid = #{cityid},
            </if>
        </set>
        where areaid = #{areaid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from tb_areas where areaid = #{areaid}
    </delete>

</mapper>


<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.changgou.order.dao.PreferentialDao">

    <resultMap type="com.changgou.order.pojo.Preferential" id="PreferentialMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="buyMoney" column="buy_money" jdbcType="INTEGER"/>
        <result property="preMoney" column="pre_money" jdbcType="INTEGER"/>
        <result property="categoryId" column="category_id" jdbcType="INTEGER"/>
        <result property="startTime" column="start_time" jdbcType="TIMESTAMP"/>
        <result property="endTime" column="end_time" jdbcType="TIMESTAMP"/>
        <result property="state" column="state" jdbcType="VARCHAR"/>
        <result property="type" column="type" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="PreferentialMap">
        select
          id, buy_money, pre_money, category_id, start_time, end_time, state, type
        from tb_preferential
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="PreferentialMap">
        select
          id, buy_money, pre_money, category_id, start_time, end_time, state, type
        from tb_preferential
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="buyMoney != null">
                and buy_money = #{buyMoney}
            </if>
            <if test="preMoney != null">
                and pre_money = #{preMoney}
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="startTime != null">
                and start_time = #{startTime}
            </if>
            <if test="endTime != null">
                and end_time = #{endTime}
            </if>
            <if test="state != null and state != ''">
                and state = #{state}
            </if>
            <if test="type != null and type != ''">
                and type = #{type}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from tb_preferential
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="buyMoney != null">
                and buy_money = #{buyMoney}
            </if>
            <if test="preMoney != null">
                and pre_money = #{preMoney}
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="startTime != null">
                and start_time = #{startTime}
            </if>
            <if test="endTime != null">
                and end_time = #{endTime}
            </if>
            <if test="state != null and state != ''">
                and state = #{state}
            </if>
            <if test="type != null and type != ''">
                and type = #{type}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into tb_preferential(buy_money, pre_money, category_id, start_time, end_time, state, type)
        values (#{buyMoney}, #{preMoney}, #{categoryId}, #{startTime}, #{endTime}, #{state}, #{type})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into tb_preferential(buy_money, pre_money, category_id, start_time, end_time, state, type)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.buyMoney}, #{entity.preMoney}, #{entity.categoryId}, #{entity.startTime}, #{entity.endTime}, #{entity.state}, #{entity.type})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into tb_preferential(buy_money, pre_money, category_id, start_time, end_time, state, type)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.buyMoney}, #{entity.preMoney}, #{entity.categoryId}, #{entity.startTime}, #{entity.endTime}, #{entity.state}, #{entity.type})
        </foreach>
        on duplicate key update
        buy_money = values(buy_money),
        pre_money = values(pre_money),
        category_id = values(category_id),
        start_time = values(start_time),
        end_time = values(end_time),
        state = values(state),
        type = values(type)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update tb_preferential
        <set>
            <if test="buyMoney != null">
                buy_money = #{buyMoney},
            </if>
            <if test="preMoney != null">
                pre_money = #{preMoney},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="startTime != null">
                start_time = #{startTime},
            </if>
            <if test="endTime != null">
                end_time = #{endTime},
            </if>
            <if test="state != null and state != ''">
                state = #{state},
            </if>
            <if test="type != null and type != ''">
                type = #{type},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from tb_preferential where id = #{id}
    </delete>

</mapper>

